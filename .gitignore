# Local development files (not for deployment)
_local/
jortt-exports/
*.xlsx
*.csv

# Server-side data (managed on server, not in repo)
data/dashboard_data.json
data/mailchimp_data.json
data/users.json
data/preferences.json
data/avatars/

# OS
.DS_Store

# Secrets (API keys, not in repo)
includes/secrets.php

# Claude Code
.claude/
